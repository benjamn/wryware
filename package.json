{
  "name": "wryware",
  "private": true,
  "author": "Ben Newman <ben@eloper.dev>",
  "description": "A collection of packages that are probably a little too clever",
  "license": "MIT",
  "scripts": {
    "prepare": "lerna exec -- npm install --ignore-scripts",
    "build": "nx run-many --target=build --all --parallel",
    "test": "nx run-many --target=test --all --parallel"
  },
  "devDependencies": {
    "@nx/js": "^16.0.2",
    "@types/mocha": "^9.1.1",
    "@types/node": "18.14.6",
    "lerna": "^6.6.1",
    "mocha": "^10.0.0",
    "nx": "15.8.4",
    "rimraf": "3.0.2",
    "rollup": "2.79.0",
    "source-map-support": "0.5.21",
    "typescript": "4.9.5"
  },
  "engines": {
    "node": ">=8"
  },
  "dependencies": {
    "tslib": "^2.4.0"
  }
}
