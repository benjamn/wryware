{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "target": "ES5",
    // We want ES2020 module syntax but everything else ES5, so Rollup can still
    // perform tree-shaking using the ESM syntax (ES2020 for dynamic import()).
    "module": "ES2020",
    "outDir": "lib/es5"
  }
}
